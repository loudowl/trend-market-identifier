const fetchReportData = async (type) => {
  // Mock implementation, replace with actual logic
  return [
    { title: `Sample Report ${type}`, description: `Report data for ${type}` },
  ];
};

module.exports = { fetchReportData };
